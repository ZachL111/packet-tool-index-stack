# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its cli tools focus without claiming live deployment or external usage.

## Cases

- `baseline`: `file span`, score 124, lane `watch`
- `stress`: `terminal width`, score 133, lane `watch`
- `edge`: `argument risk`, score 156, lane `ship`
- `recovery`: `report density`, score 218, lane `ship`
- `stale`: `file span`, score 168, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
