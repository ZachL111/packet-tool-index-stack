# Packet Tool Index Stack Walkthrough

The fixture is intentionally compact, so the review starts with the cases that pull farthest apart.

| Case | Focus | Score | Lane |
| --- | --- | ---: | --- |
| baseline | file span | 124 | watch |
| stress | terminal width | 133 | watch |
| edge | argument risk | 156 | ship |
| recovery | report density | 218 | ship |
| stale | file span | 168 | ship |

Start with `recovery` and `baseline`. They create the widest contrast in this repository's fixture set, which makes them better review anchors than the middle cases.

The useful comparison is `report density` against `file span`, not the raw score alone.
