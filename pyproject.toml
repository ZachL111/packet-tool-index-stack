[project]
name = "packet-tool-index-stack"
version = "0.1.0"
description = "Package a Python local lab for index analysis with append-only fixtures, checkpoint recovery checks, and documented operating limits."
requires-python = ">=3.11"
